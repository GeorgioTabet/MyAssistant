import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function TodayScreen() {
  return (
    <PlaceholderScreen
      title="Today"
      icon="house.fill"
      phase="PHASE 5"
      description="Your daily dashboard — today's tasks and a morning briefing — lands here."
    />
  );
}
