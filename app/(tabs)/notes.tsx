import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function NotesScreen() {
  return (
    <PlaceholderScreen
      title="Notes"
      icon="note.text"
      phase="PHASE 4"
      description="Write notes or capture them by voice — speak a thought and it becomes text."
    />
  );
}
