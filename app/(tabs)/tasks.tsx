import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function TasksScreen() {
  return (
    <PlaceholderScreen
      title="Tasks"
      icon="checklist"
      phase="PHASE 3"
      description="Add, complete, and delete tasks — with reminders that notify you at the due time."
    />
  );
}
